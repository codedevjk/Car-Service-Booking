import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-service-list',
  templateUrl: './service-list.component.html',
  styleUrls: ['./service-list.component.css']
})
export class ServiceListComponent implements OnInit {

  // TODO: Trainee to implement US05 (Car Service Management)
  // 1. Inject CarServiceService and CategoryService.
  // 2. Initialize a Reactive Form for adding/editing a car service.
  // 3. Load categories to populate the dropdown.
  // 4. Load all car services from the backend and store them in an array.
  // 5. Implement logic to add, edit, and delete car services.

  constructor() {}

  ngOnInit(): void {
    // TODO: Initialize form and load initial data
  }
}
